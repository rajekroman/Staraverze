{
  "name": "lovec-vltavinu-reborn",
  "version": "5.4.2",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node tools/validate.mjs",
    "test:browser": "playwright test",
    "test:smoke": "playwright test tests/game-smoke.spec.mjs",
    "test:offline": "playwright test --project=offline-chromium",
    "test:visual": "playwright test tests/visual-regression.spec.mjs",
    "test": "npm run validate && npm run test:browser"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
